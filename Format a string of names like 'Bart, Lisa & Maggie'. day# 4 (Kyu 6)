list =(names)=>{

/////Kasutan Map() ja regEx 
  return names.map(function(x){ return x.name; }).join(", ").replace(/,(?!.*,)/gmi, " &");
} 

list([ {name: 'Bart'}, {name: 'Lisa'}, {name: 'Maggie'} ])
# returns 'Bart, Lisa & Maggie'

list([ {name: 'Bart'}, {name: 'Lisa'} ])
# returns 'Bart & Lisa'

list([ {name: 'Bart'} ])
# returns 'Bart'
